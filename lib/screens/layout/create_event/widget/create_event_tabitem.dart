import 'package:flutter/material.dart';

class CreateEventTabitem extends StatelessWidget {
  const CreateEventTabitem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
